spring:
  cloud:
    propertiesvolume:
      enabled: true
      prefixed: false
      paths: /mnt/secrets/wa
      aliases:
        wa.testsecret: AN_ALIASED_VALUE
        wa.AppInsightsInstrumentationKey: azure.application-insights.instrumentation-key
        s2s.microservicekey-wa-workflow-api: S2S_SECRET_WORKFLOW_API
